{
  "name": "online-watching",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:client": "npm run dev -w apps/client",
    "dev:server": "npm run dev -w apps/server",
    "dev:public": "concurrently -n server,client -c green,magenta \"npm:dev:server:public\" \"npm:dev:client:public\"",
    "dev:public:ip": "sh -c 'PUBLIC_HOST=${PUBLIC_HOST:-185.19.5.87}; echo \"Public URL: http://$PUBLIC_HOST:5173\"; echo \"API URL: http://$PUBLIC_HOST:4000\"; VITE_API_URL=http://$PUBLIC_HOST:4000 CLIENT_ORIGIN=http://$PUBLIC_HOST:5173,http://localhost:5173,http://127.0.0.1:5173 concurrently -n server,client -c green,magenta \"npm:dev:server:public\" \"npm:dev:client:public\"'",
    "dev:client:public": "npm run dev:public -w apps/client",
    "dev:server:public": "npm run dev:public -w apps/server",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate -w apps/server",
    "prisma:migrate": "npm run prisma:migrate -w apps/server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
